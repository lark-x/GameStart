# ADR-0002：原生 HTTP API

- 状态：已接受
- 日期：2026-08-12

## 决策

当前 API 运行时使用 Node.js `node:http`，由路由分发、请求解析、Use Case 和响应映射组成。Fastify 不是当前依赖，也不是默认迁移目标。

## 原因与影响

现有实现已经具备可测试的请求处理、SSE、CORS、错误映射和运行时组装。引入 HTTP 框架必须解决明确问题，并通过独立 ADR 评估迁移成本、Contract 兼容和测试影响；不得因旧文档描述而直接引入。
