# ADR-0005：外部生成服务信任边界

- 状态：已接受
- 日期：2026-08-12

## 决策

LLM 和 ComfyUI 均视为不可信外部适配器。模型可生成文本、结构化候选、记忆候选和图片意图，但不能直接修改关系、资产、世界状态或绕过发布审核。

## 强制边界

所有调用必须具备超时、错误归一化、秘密脱敏和测试替身；所有结果必须经过结构解析、领域校验、权限与可见性检查、幂等控制，并按功能开关决定自动应用或进入人工审核。默认自动测试不得调用真实供应商。
